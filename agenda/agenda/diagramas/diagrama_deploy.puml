@startuml diagrama_deploy
' Thema para um visual mais moderno
!theme vibrant

title Diagrama de Implantação - Afrodite Calendar no Heroku

' Usa linhas retas para organizar as conexões
skinparam linetype ortho

!define DEVICONS https://raw.githubusercontent.com/tupadr3/plantuml-icon-font-sprites/master/devicons
!define FONTAWESOME https://raw.githubusercontent.com/tupadr3/plantuml-icon-font-sprites/master/font-awesome-5
!include DEVICONS/firefox.puml
!include DEVICONS/java.puml
!include DEVICONS/heroku.puml
!include FONTAWESOME/database.puml
!include FONTAWESOME/user_shield.puml

actor "Usuário" as user

node "Dispositivo do Cliente" {
  agent "Browser" as browser <<$firefox>>
}

cloud "Nuvem (Internet)" {
    
    node "Heroku" as heroku_node <<$heroku>> {
        artifact "Afrodite Calendar App\n(Spring Boot JAR)" as app <<$java>> {
            component "Frontend Estático\n(HTML, CSS, JS)" as frontend
            component "API REST\n(Controllers)" as api
            component "Lógica da Agenda\n(Models)" as core
        }
    }
    
    package "Google Firebase" {
        node "Firebase Authentication" as auth <<$user_shield>>
        database "Cloud Firestore" as firestore <<$database>>
    }
}

user -down-> browser : "1. Acessa a aplicação"

' Fluxo de dados entre Browser e App
app -up-> browser : "2. Serve arquivos do Frontend (HTML, CSS)"
browser -down-> app : "3. Envia requisições para a API (com Token JWT)"

' Fluxo Backend com os serviços do Firebase
app -right-> auth : "4. Valida Token JWT (Admin SDK)"
app -right-> firestore : "5. Lê e escreve dados dos contatos (Admin SDK)"

note right of app
  A aplicação Spring Boot roda
  em um container "Dyno",
  servindo tanto o frontend estático
  quanto a API REST.
end note

@enduml